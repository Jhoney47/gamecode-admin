# GameCode 后台管理系统 - 完整使用说明

## 📋 目录
1. [系统概述](#系统概述)
2. [后台管理操作](#后台管理操作)
3. [前端APP集成](#前端app集成)
4. [自动同步机制](#自动同步机制)
5. [常见问题](#常见问题)

---

## 系统概述

### 🎯 核心功能
- ✅ 游戏和兑换码管理
- ✅ 截止日期设置
- ✅ 即将过期分类（7天内）
- ✅ 最新发布分类（7天内）
- ✅ 准确率等爬取数据字段
- ✅ 自动同步到GitHub和CDN
- ✅ 国内无需梯子访问

### 🔗 访问地址
- **后台管理：** https://8888-ih8el24d8dxezasg9avep-f0fdca25.sg1.manus.computer
- **GitHub仓库：** https://github.com/Jhoney47/gamecode-admin
- **CDN数据（国内可用）：** https://cdn.jsdelivr.net/gh/Jhoney47/gamecode-admin@master/GameCodeBase.json

---

## 后台管理操作

### 1. 查看数据统计

左侧边栏显示实时统计：
- **总游戏数**：当前管理的游戏总数
- **总兑换码数**：所有游戏的兑换码总数
- **即将过期**：7天内即将过期的兑换码数量
- **最新发布**：7天内发布的兑换码数量
- **平均准确率**：所有兑换码的平均准确率

### 2. 分类筛选

点击左侧分类按钮：
- **📋 全部兑换码**：显示所有兑换码
- **⏰ 即将过期 (7天内)**：只显示7天内即将过期的兑换码
- **🆕 最新发布 (7天内)**：只显示7天内发布的兑换码

### 3. 选择游戏

使用左侧下拉框选择特定游戏：
- 选择后只显示该游戏的兑换码
- 显示格式：`游戏名 (兑换码数量)`

### 4. 搜索功能

右上角搜索框：
- 输入兑换码或奖励描述关键词
- 实时过滤显示结果

### 5. 添加游戏

点击"➕ 添加游戏"按钮：
1. 输入游戏名称
2. 点击"添加"
3. 系统自动保存并同步

### 6. 添加兑换码

点击"➕ 添加兑换码"按钮：

**必填字段：**
- 选择游戏
- 兑换码
- 奖励描述

**可选字段：**
- 来源平台
- 来源URL
- **截止日期**（留空表示永久有效）
- 爬取来源（手动/自动/API）
- 准确率（0-100）
- 验证成功率（0-100）

**操作步骤：**
1. 填写所有字段
2. 点击"添加"
3. 系统自动保存并同步到GitHub

### 7. 查看兑换码详情

点击表格中的"查看"按钮：

**显示信息：**
- 兑换码（蓝色大字）
- 游戏名称
- 奖励描述
- 状态徽章
- **截止日期**（颜色标识）
- 来源平台
- 来源URL（可点击）
- 爬取来源
- 爬取时间
- 准确率（颜色徽章）
- 验证成功率
- 发布时间

### 8. 编辑兑换码

点击表格中的"编辑"按钮：
1. 修改任意字段
2. **可以修改截止日期**
3. 点击"保存"
4. 系统自动保存并同步

### 9. 删除兑换码

点击表格中的"删除"按钮：
1. 确认删除
2. 系统自动保存并同步

### 10. 刷新数据

点击"🔄 刷新数据"按钮：
- 重新加载最新数据
- 更新统计信息

---

## 前端APP集成

### 🇨🇳 国内可用的CDN地址

```
https://cdn.jsdelivr.net/gh/Jhoney47/gamecode-admin@master/GameCodeBase.json
```

**特点：**
- ✅ 国内可直接访问，无需梯子
- ✅ 速度快（CDN加速）
- ✅ 免费稳定

### 📱 APP端集成代码

#### 方法1：简单版本（推荐）

```javascript
const CDN_URL = 'https://cdn.jsdelivr.net/gh/Jhoney47/gamecode-admin@master/GameCodeBase.json';

async function getGameCodes() {
  try {
    // 添加时间戳参数，强制刷新缓存
    const timestamp = Date.now();
    const url = `${CDN_URL}?t=${timestamp}`;
    
    const response = await fetch(url);
    const data = await response.json();
    
    console.log('数据版本:', data.version);
    console.log('更新时间:', data.lastUpdated);
    console.log('游戏数量:', data.games.length);
    
    return data;
  } catch (error) {
    console.error('获取数据失败:', error);
    throw error;
  }
}

// 使用示例
getGameCodes().then(data => {
  data.games.forEach(game => {
    console.log(`游戏: ${game.gameName}`);
    game.codes.forEach(code => {
      console.log(`  兑换码: ${code.code}`);
      console.log(`  准确率: ${code.accuracyRate}%`);
      console.log(`  截止日期: ${code.expireDate || '永久有效'}`);
    });
  });
});
```

#### 方法2：筛选即将过期（7天内）

```javascript
async function getExpiringSoonCodes() {
  const data = await getGameCodes();
  
  const now = new Date();
  const sevenDaysLater = new Date(now.getTime() + 7 * 24 * 60 * 60 * 1000);
  
  const expiringSoon = [];
  
  data.games.forEach(game => {
    game.codes.forEach(code => {
      if (code.expireDate) {
        const expireDate = new Date(code.expireDate);
        // 未过期且在7天内
        if (expireDate > now && expireDate <= sevenDaysLater) {
          expiringSoon.push({
            ...code,
            gameName: game.gameName,
            daysLeft: Math.ceil((expireDate - now) / (1000 * 60 * 60 * 24))
          });
        }
      }
    });
  });
  
  return expiringSoon;
}

// 使用示例
getExpiringSoonCodes().then(codes => {
  console.log(`即将过期的兑换码 (${codes.length}个):`);
  codes.forEach(code => {
    console.log(`${code.code} - 还有${code.daysLeft}天过期`);
  });
});
```

#### 方法3：筛选最新发布（7天内）

```javascript
async function getRecentlyAddedCodes() {
  const data = await getGameCodes();
  
  const now = new Date();
  const sevenDaysAgo = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000);
  
  const recentlyAdded = [];
  
  data.games.forEach(game => {
    game.codes.forEach(code => {
      const publishDate = new Date(code.publishDate);
      if (publishDate >= sevenDaysAgo) {
        recentlyAdded.push({
          ...code,
          gameName: game.gameName
        });
      }
    });
  });
  
  // 按发布时间倒序排列
  recentlyAdded.sort((a, b) => 
    new Date(b.publishDate) - new Date(a.publishDate)
  );
  
  return recentlyAdded;
}

// 使用示例
getRecentlyAddedCodes().then(codes => {
  console.log(`最新发布的兑换码 (${codes.length}个):`);
  codes.forEach(code => {
    const publishDate = new Date(code.publishDate);
    console.log(`${code.code} - ${publishDate.toLocaleDateString('zh-CN')}`);
  });
});
```

#### 方法4：React Native完整示例

```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, FlatList, RefreshControl, StyleSheet } from 'react-native';
import AsyncStorage from '@react-native-async-storage/async-storage';

const CDN_URL = 'https://cdn.jsdelivr.net/gh/Jhoney47/gamecode-admin@master/GameCodeBase.json';

export default function GameCodeScreen() {
  const [games, setGames] = useState([]);
  const [loading, setLoading] = useState(false);
  const [version, setVersion] = useState(null);
  const [expiringSoonCount, setExpiringSoonCount] = useState(0);
  const [recentCount, setRecentCount] = useState(0);

  // 加载数据
  const loadData = async (forceRefresh = false) => {
    try {
      setLoading(true);
      
      // 添加时间戳强制刷新
      const timestamp = Date.now();
      const url = `${CDN_URL}?t=${timestamp}`;
      
      const response = await fetch(url);
      const data = await response.json();
      
      // 检查版本号
      const cachedVersion = await AsyncStorage.getItem('gameCodeVersion');
      
      if (data.version !== cachedVersion || forceRefresh) {
        console.log('数据已更新');
        setGames(data.games);
        setVersion(data.version);
        
        // 计算统计数据
        calculateStats(data);
        
        // 保存到本地
        await AsyncStorage.setItem('gameCodeData', JSON.stringify(data));
        await AsyncStorage.setItem('gameCodeVersion', String(data.version));
      } else {
        console.log('数据未变化');
      }
      
    } catch (error) {
      console.error('加载数据失败:', error);
      
      // 从本地加载
      const localData = await AsyncStorage.getItem('gameCodeData');
      if (localData) {
        const data = JSON.parse(localData);
        setGames(data.games);
        setVersion(data.version);
        calculateStats(data);
      }
    } finally {
      setLoading(false);
    }
  };

  // 计算统计数据
  const calculateStats = (data) => {
    const now = new Date();
    const sevenDaysLater = new Date(now.getTime() + 7 * 24 * 60 * 60 * 1000);
    const sevenDaysAgo = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000);
    
    let expiring = 0;
    let recent = 0;
    
    data.games.forEach(game => {
      game.codes.forEach(code => {
        // 即将过期
        if (code.expireDate) {
          const expireDate = new Date(code.expireDate);
          if (expireDate > now && expireDate <= sevenDaysLater) {
            expiring++;
          }
        }
        
        // 最新发布
        const publishDate = new Date(code.publishDate);
        if (publishDate >= sevenDaysAgo) {
          recent++;
        }
      });
    });
    
    setExpiringSoonCount(expiring);
    setRecentCount(recent);
  };

  // 首次加载
  useEffect(() => {
    loadData();
  }, []);

  // 下拉刷新
  const onRefresh = () => {
    loadData(true);
  };

  // 渲染兑换码状态
  const renderStatus = (code) => {
    const now = new Date();
    
    // 检查是否过期
    if (code.expireDate) {
      const expireDate = new Date(code.expireDate);
      if (expireDate < now) {
        return <Text style={styles.statusExpired}>已过期</Text>;
      }
      
      // 检查是否即将过期
      const sevenDaysLater = new Date(now.getTime() + 7 * 24 * 60 * 60 * 1000);
      if (expireDate <= sevenDaysLater) {
        const daysLeft = Math.ceil((expireDate - now) / (1000 * 60 * 60 * 24));
        return <Text style={styles.statusExpiring}>即将过期 ({daysLeft}天)</Text>;
      }
    }
    
    // 检查是否最新发布
    const publishDate = new Date(code.publishDate);
    const sevenDaysAgo = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000);
    if (publishDate >= sevenDaysAgo) {
      return <Text style={styles.statusNew}>🆕 最新</Text>;
    }
    
    return <Text style={styles.statusActive}>有效</Text>;
  };

  return (
    <View style={styles.container}>
      {/* 统计卡片 */}
      <View style={styles.statsContainer}>
        <View style={styles.statCard}>
          <Text style={styles.statLabel}>总游戏数</Text>
          <Text style={styles.statValue}>{games.length}</Text>
        </View>
        <View style={styles.statCard}>
          <Text style={styles.statLabel}>即将过期</Text>
          <Text style={[styles.statValue, styles.statWarning]}>{expiringSoonCount}</Text>
        </View>
        <View style={styles.statCard}>
          <Text style={styles.statLabel}>最新发布</Text>
          <Text style={[styles.statValue, styles.statSuccess]}>{recentCount}</Text>
        </View>
      </View>
      
      {/* 游戏列表 */}
      <FlatList
        data={games}
        keyExtractor={(item) => item.gameName}
        refreshControl={
          <RefreshControl refreshing={loading} onRefresh={onRefresh} />
        }
        renderItem={({ item }) => (
          <View style={styles.gameCard}>
            <Text style={styles.gameName}>{item.gameName}</Text>
            <Text style={styles.codeCount}>{item.codeCount} 个兑换码</Text>
            
            {item.codes.map((code) => (
              <View key={code.code} style={styles.codeItem}>
                <View style={styles.codeHeader}>
                  <Text style={styles.code}>{code.code}</Text>
                  {renderStatus(code)}
                </View>
                <Text style={styles.reward}>{code.rewardDescription}</Text>
                {code.expireDate && (
                  <Text style={styles.expireDate}>
                    截止: {new Date(code.expireDate).toLocaleDateString('zh-CN')}
                  </Text>
                )}
                {code.accuracyRate && (
                  <Text style={styles.accuracy}>准确率: {code.accuracyRate}%</Text>
                )}
              </View>
            ))}
          </View>
        )}
      />
      
      {/* 版本信息 */}
      <Text style={styles.version}>版本: {version}</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f5f5f5',
  },
  statsContainer: {
    flexDirection: 'row',
    padding: 16,
    backgroundColor: '#fff',
  },
  statCard: {
    flex: 1,
    alignItems: 'center',
  },
  statLabel: {
    fontSize: 12,
    color: '#666',
    marginBottom: 4,
  },
  statValue: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#333',
  },
  statWarning: {
    color: '#ffc107',
  },
  statSuccess: {
    color: '#28a745',
  },
  gameCard: {
    backgroundColor: '#fff',
    margin: 8,
    padding: 16,
    borderRadius: 8,
  },
  gameName: {
    fontSize: 18,
    fontWeight: 'bold',
    marginBottom: 8,
  },
  codeCount: {
    fontSize: 14,
    color: '#666',
    marginBottom: 12,
  },
  codeItem: {
    borderTopWidth: 1,
    borderTopColor: '#eee',
    paddingTop: 8,
    marginTop: 8,
  },
  codeHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 4,
  },
  code: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#007bff',
  },
  statusActive: {
    fontSize: 12,
    color: '#28a745',
  },
  statusExpired: {
    fontSize: 12,
    color: '#dc3545',
  },
  statusExpiring: {
    fontSize: 12,
    color: '#ffc107',
  },
  statusNew: {
    fontSize: 12,
    color: '#007bff',
  },
  reward: {
    fontSize: 14,
    color: '#333',
    marginTop: 4,
  },
  expireDate: {
    fontSize: 12,
    color: '#666',
    marginTop: 4,
  },
  accuracy: {
    fontSize: 12,
    color: '#28a745',
    marginTop: 4,
  },
  version: {
    textAlign: 'center',
    padding: 8,
    fontSize: 12,
    color: '#999',
  },
});
```

### 📊 数据结构说明

```json
{
  "version": 1737457066933,  // 版本号（时间戳）
  "lastUpdated": "2026-01-21T11:37:46.933Z",  // 最后更新时间
  "games": [
    {
      "gameName": "铃兰之剑",
      "codeCount": 45,
      "codes": [
        {
          "code": "KSTGIFT",  // 兑换码
          "rewardDescription": "钻石*100",  // 奖励描述
          "sourcePlatform": "官方",  // 来源平台
          "sourceUrl": "https://example.com",  // 来源URL
          "expireDate": "2026-03-31",  // 截止日期（新增）
          "status": "active",  // 状态
          "codeType": "permanent",  // 类型
          "publishDate": "2026-01-21T10:00:00Z",  // 发布时间
          "verificationCount": 0,  // 验证次数
          "reviewStatus": "approved",  // 审核状态
          "crawlSource": "manual",  // 爬取来源
          "crawlTime": "2026-01-21T10:00:00Z",  // 爬取时间
          "accuracyRate": 95,  // 准确率
          "verificationSuccessRate": 90,  // 验证成功率
          "lastVerifiedTime": null,  // 最后验证时间
          "crawlerVersion": null  // 爬虫版本
        }
      ]
    }
  ]
}
```

---

## 自动同步机制

### 🔄 完整流程

```
1. 后台修改数据
   ↓ (立即)
2. 保存到JSON文件 + 更新版本号
   ↓ (1秒)
3. 自动推送到GitHub
   ↓ (立即)
4. 自动清除CDN缓存
   ↓ (1-2分钟)
5. CDN更新完成
   ↓ (用户刷新)
6. APP获取最新数据
```

### ⏱️ 时间线

- **T+0秒**：后台修改
- **T+1秒**：GitHub同步
- **T+1-2分钟**：CDN更新完成
- **T+刷新**：APP显示最新数据

### 📝 同步日志

查看同步日志：
```
http://localhost:8888/api/sync-log
```

或查看文件：
```
/home/ubuntu/gamecode_admin/sync.log
```

### ✅ 您需要做什么？

**答案：什么都不用做！**

1. ✅ 在后台添加/编辑/删除兑换码
2. ✅ 点击保存按钮
3. ✅ **其他全部自动完成**

---

## 常见问题

### Q1: 是否需要重新下载前端软件？

**答案：不需要！**

- 前端软件从CDN获取JSON数据
- JSON数据结构可以动态扩展
- 只要前端代码支持读取新字段，就会自动显示
- **无需重新下载或更新APP**

### Q2: 后台增删改查后需要做什么？

**答案：什么都不用做！**

完全自动化流程：
1. ✅ 您在后台修改数据
2. ✅ 系统自动保存到JSON文件
3. ✅ 1秒后自动推送到GitHub
4. ✅ 自动清除CDN缓存
5. ✅ 1-2分钟后CDN更新完成
6. ✅ 用户刷新APP即可看到最新数据

### Q3: 为什么APP还是显示旧数据？

**可能原因：**
1. CDN缓存还没更新（等待1-2分钟）
2. APP使用了本地缓存（强制刷新）

**解决方法：**
1. 等待1-2分钟后刷新
2. APP端添加时间戳参数：`?t=${Date.now()}`
3. 检查版本号是否变化

### Q4: 国内访问会被墙吗？

**答案：不会！**

- jsDelivr CDN在国内有节点
- 可以直接访问，无需梯子
- 速度快，稳定可靠

### Q5: 如何确保获取到最新数据？

**方法：**
1. 使用版本号控制
2. 添加时间戳参数
3. 检查`lastUpdated`字段

**示例代码：**
```javascript
const url = `${CDN_URL}?t=${Date.now()}`;
const response = await fetch(url);
const data = await response.json();

// 检查版本号
if (data.version !== cachedVersion) {
  // 有更新，使用新数据
}
```

### Q6: 如何筛选即将过期的兑换码？

**代码示例：**
```javascript
const now = new Date();
const sevenDaysLater = new Date(now.getTime() + 7 * 24 * 60 * 60 * 1000);

const expiringSoon = [];
data.games.forEach(game => {
  game.codes.forEach(code => {
    if (code.expireDate) {
      const expireDate = new Date(code.expireDate);
      if (expireDate > now && expireDate <= sevenDaysLater) {
        expiringSoon.push(code);
      }
    }
  });
});
```

### Q7: 如何筛选最新发布的兑换码？

**代码示例：**
```javascript
const sevenDaysAgo = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000);

const recentlyAdded = [];
data.games.forEach(game => {
  game.codes.forEach(code => {
    const publishDate = new Date(code.publishDate);
    if (publishDate >= sevenDaysAgo) {
      recentlyAdded.push(code);
    }
  });
});
```

### Q8: 如何判断兑换码状态？

**代码示例：**
```javascript
function getCodeStatus(code) {
  const now = new Date();
  
  // 检查是否过期
  if (code.expireDate) {
    const expireDate = new Date(code.expireDate);
    if (expireDate < now) {
      return '已过期';
    }
    
    // 检查是否即将过期（7天内）
    const sevenDaysLater = new Date(now.getTime() + 7 * 24 * 60 * 60 * 1000);
    if (expireDate <= sevenDaysLater) {
      const daysLeft = Math.ceil((expireDate - now) / (1000 * 60 * 60 * 24));
      return `即将过期 (${daysLeft}天)`;
    }
  }
  
  // 检查是否最新发布（7天内）
  const publishDate = new Date(code.publishDate);
  const sevenDaysAgo = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000);
  if (publishDate >= sevenDaysAgo) {
    return '🆕 最新';
  }
  
  return '有效';
}
```

---

## 📞 技术支持

如有问题，请查看：
1. **同步日志**：http://localhost:8888/api/sync-log
2. **GitHub仓库**：https://github.com/Jhoney47/gamecode-admin
3. **测试报告**：`/home/ubuntu/gamecode_admin/新功能测试报告.md`

---

**祝使用愉快！** 🎉
